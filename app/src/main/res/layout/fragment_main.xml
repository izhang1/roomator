<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" tools:context="com.example.izhang.roomator.mainFrag">

    <!-- TODO: Update blank fragment layout -->

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/accent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:text="Welcome"
            android:id="@+id/mainTitleView"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="20dp"
            android:textStyle="bold"
            android:textColor="@color/text"
            android:textSize="70dp"
            android:textAlignment="center"
            android:gravity="center" />

        <ListView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/mainFragList"
            android:layout_below="@+id/mainTitleView"
            android:layout_centerHorizontal="true"
            android:elevation="6dp"
            android:layout_marginLeft="20dp"
            android:drawSelectorOnTop="false"
            android:dividerHeight="1.5dp"
            android:divider="@color/primary" />

    </RelativeLayout>
</FrameLayout>
